{
  "name": "mindarmor",
  "version": "1.0.0",
  "description": "MindArmor — a mental health & resilience dashboard with stress tracking, grounding/breathing reset tools, and an educational resources library.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node scripts/build-messages.js && node --check server.js && node --check reframe.js && node --check public/app.js && node --check public/data.js && node --check public/stressStore.js && echo \"MindArmor: all sources valid\""
  },
  "keywords": [
    "mental-health",
    "resilience",
    "wellbeing",
    "dashboard"
  ],
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "express": "^4.21.2"
  }
}
